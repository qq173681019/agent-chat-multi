# Dependencies
node_modules/
data/
*.log
.DS_Store
.env

# Local configs (secrets — keep out of repo)
config.json
config-b.json
cloudflared.exe
cloudflared-token.txt
.poller_*_lastid
ws-url.json
.bak-preagentchat
*.bak-preagentchat
*.bak-20260606-*

# Secrets (推荐放在仓库外，比如 ~/.agent-chat-secrets.json)
.agent-chat-secrets.json
secrets.json
*secrets*.json

# Python
__pycache__/
*.pyc

# 本地 cloudflared 二进制（不在 git 里追踪，去 https://github.com/cloudflare/cloudflared 下载）
cloudflared
cloudflared.exe

# Agent poller state files (per-agent)
.last_id_*
.poller_*.pid
